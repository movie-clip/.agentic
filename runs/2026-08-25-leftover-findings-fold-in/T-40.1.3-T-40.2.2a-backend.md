REPORT 2026-08-25-leftover-findings-fold-in/T-40.1.3+T-40.2.2a
status:      DONE
verdict:     NONE

changed:
  - services/quant-engine/app/scripts/frozen_market_data.py — RecordingMarketData gains get_company_profile/get_etf_sector_weightings (delegate + capture, matching MarketDataService's real signatures)
  - services/quant-engine/app/scripts/frozen_market_data.py — __init__ gains self._profiles/self._sector_weightings dicts; to_payload() gains "profiles"/"sector_weightings" sorted-dict keys
  - services/quant-engine/app/schemas/imports.py — new CombineImportedSnapshotsRequest(BaseModel): snapshots: list[ImportedPortfolioSnapshot], pure wrapper, no new nested types
  - services/quant-engine/app/api/routes/imports.py — new POST /portfolios/import/combine-snapshots route calling combine_imported_snapshots(request.snapshots) verbatim
  - services/quant-engine/app/api/routes/imports.py — new route's error mapping: try/except ValueError -> HTTPException(400), mirroring import_interactive_brokers_statement's exact pattern

verification:
  command:   cd services/quant-engine && pytest -k "frozen_market_data or import_engine or statement_importer or imports" -q
  result:    PASS
  detail:    4 passed, 901 deselected (python -m pytest, "pytest" alone not on PATH in this shell). No test file matches "frozen_market_data", "import_engine", or "statement_importer" as literal substrings today — the 4 collected tests (test_importer.py, test_importer_csv.py) matched via "imports" appearing inside test function names, not file/module names under those exact filter terms; confirmed via --collect-only. This is expected: T-40.1.4/T-40.2.3 (test-engineer, out of this order's scope) are the dispatches that add coverage for frozen_market_data.py and the new combine-snapshots route. Additionally ran (not the named command, supplementary sanity checks): `python -c "from app.api.main import app"` — imports cleanly, 28 routes registered, confirming no new include_router call was needed; `python scripts/detect_deadcode.py --strict` — clean, zero findings.

contract_notes:
  - app/schemas/imports.py changed (new CombineImportedSnapshotsRequest) — TS type in apps/desktop/src/features/portfolio/types.ts owned by T-40.2.2b (frontend-engineer), not this ticket.
  - docs/contracts/ needs a new entry for POST /portfolios/import/combine-snapshots (docs-engineer's call on which file) — owned by docs-engineer close-out, not this ticket.
  - schema_edit_reminder.py hook fired on the app/schemas/imports.py edit as expected; both downstream artifacts above are named per that reminder.

pack_corrections:
  - none

handoff:
  - New route: POST /portfolios/import/combine-snapshots, body {"snapshots": [ImportedPortfolioSnapshot, ...]}, response_model=ImportedPortfolioSnapshot, 400 on ValueError with detail=str(exc).
  - T-40.2.2b's adapter fn should POST to /api/portfolios/import/combine-snapshots per 05-technical-plan.md § US-40.2 design.
  - Router is the existing `router` object in app/api/routes/imports.py, already registered via main.py:19 — no new registration was added or needed.
  - RecordingMarketData's to_payload() now emits "profiles"/"sector_weightings" keys too — FrozenMarketData (replay side) intentionally untouched per the plan's scope boundary.
  - No test files were touched, per non_goals — T-40.1.4 and T-40.2.3 are the dispatches that add coverage for both changes in this report.

risks:
  - none
