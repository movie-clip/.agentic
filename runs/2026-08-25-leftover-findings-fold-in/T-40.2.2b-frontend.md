REPORT 2026-08-25-leftover-findings-fold-in/T-40.2.2b
status:      PARTIAL
verdict:     NONE

changed:
  - apps/desktop/src/features/portfolio/portfolioAnalysisAdapter.ts — added combineImportedSnapshots(snapshots, apiUrlOptions?), POSTs {snapshots} to /api/portfolios/import/combine-snapshots
  - apps/desktop/src/app/App.tsx — imported combineImportedSnapshots from portfolioAnalysisAdapter
  - apps/desktop/src/app/App.tsx — add_snapshot branch computes baseImportedHistorySnapshot via canUseImportedReplay(baseSource)
  - apps/desktop/src/app/App.tsx — add_snapshot branch calls combineImportedSnapshots in try/catch; catch sets combinedHistorySnapshot null + setImportError(...)
  - apps/desktop/src/app/App.tsx — saveImportedSnapshotNode(...) now receives importedHistorySnapshot: combinedHistorySnapshot, replacing the literal null

verification:
  command:   cd apps/desktop && npx tsc --noEmit 2>&1 | tail -40 ; npx vitest run 2>&1 | tail -80
  result:    FAIL
  detail:    tsc clean (no output). vitest: 1 test file failed, 37 passed; 2 tests failed, 339 passed (341 total). Both failures pre-date/are-caused-by other-lane work, not a defect in this ticket's logic — see risks.

contract_notes:
  - No named TS request type for CombineImportedSnapshotsRequest — adapter posts inline `{ snapshots }`, matching runImportedDiagnosticsEngine's no-named-wrapper convention.
  - ImportedSnapshot[] already mirrors ImportedPortfolioSnapshot 1:1, and the backend request is a pure wrapper with no new nested types — see risks.
  - docs/contracts/ still needs an entry for POST /portfolios/import/combine-snapshots — owned by docs-engineer close-out per 05-technical-plan.md, not this ticket.

pack_corrections:
  - none

handoff:
  - New adapter fn: combineImportedSnapshots(snapshots, apiUrlOptions?) in portfolioAnalysisAdapter.ts, mirrors runImportedDiagnosticsEngine's fetch/error-check shape.
  - T-40.2.3 must add one mocked fetch response for POST /api/portfolios/import/combine-snapshots in App.test.tsx's "refreshes dashboard...adding a statement snapshot" test.
  - That mock goes between the ffBootstrapPayload response (5th mockResolvedValueOnce) and the diagnosticsPayload response (6th) — see risks for root cause.
  - T-40.2.3's test plan (sequential-add-chain, degraded-combination, replace-mode regression) still applies verbatim against 05-technical-plan.md § US-40.2; not added here per non_goals.
  - Verified (not just trusted the design doc) saveImportedSnapshotNode, ImportedHistorySource union, buildImportedHistorySource already fully support non-null importedHistorySnapshot.
  - Verified sequential add_snapshot chains stay correct: statement_importer.py's _collect_statements reads each input's full .statements list, so chained combines accumulate.

risks:
  - Existing test "App.test.tsx > refreshes dashboard...adding a statement snapshot" now fails, caused by this ticket's change.
  - Root cause: that test's base source has historySource.kind === 'imported_replay', so add_snapshot now calls combineImportedSnapshots — an extra fetch not in its 8-entry mock chain.
  - Confirmed via isolated run: the extra fetch desyncs the mock queue; later calls fall through to real fetch, failing "Failed to parse URL from /api/engines/exposure/run".
  - This is the exact coverage gap T-40.2.3 (test-engineer) is scoped to close; this ticket's non_goals bar writing/editing tests, so App.test.tsx was left untouched.
  - Second failing test "shows base and child variant lineage in Exposure snapshot options" is unrelated to this ticket.
  - That second test expects option label 'Working Draft · base' with no date suffix, but sibling ticket T-40.1.2's variantLabels.ts change now appends one — belongs to T-40.1.4, not T-40.2.2b.
  - Per the input handoff's own suggestion, I judged CombineImportedSnapshotsRequest does not need a named TS interface; flagging this judgment call as instructed by the order.
