REPORT 2026-08-20-health-review-fold-in/07
status:      DONE
verdict:     NONE

changed:
  - scripts/audit_dependencies.py — NEW. Pure `classify(returncode, stdout, stderr) -> Outcome` (`Outcome` = `CLEAN | VULNERABILITIES_FOUND | SCAN_UNAVAILABLE`), no side effects, no import-time argv parsing or network calls. Thin `main()` shells out to `python -m pip_audit -r services/quant-engine/requirements.txt` and `npm audit --prefix apps/desktop --json`, feeding each `CompletedProcess` into `classify()`. Exit codes: 0 clean, 1 vulnerabilities found (takes priority if the two ecosystems disagree), 3 scan unavailable — distinct per the DoD.
  - services/quant-engine/requirements-dev.txt — added `pip-audit==2.10.1` alongside the existing `ruff`/`vulture` dev tooling, with a comment naming the rationale and pointing at `scripts/audit_dependencies.py`. `requirements.txt` itself left untouched, per scope.

verification:
  command:   python scripts/run_all_tests.py
  result:    PASS
  detail:    Exit 0. "782 passed, 45 warnings" (backend pytest), "37 passed (37)" test files / "331 passed (331)" tests (frontend vitest), dead-code gate (ruff+vulture+knip) clean, tsc clean, dashboard goldens regenerated and byte-identical, final line "All tests passed." This confirms no regression to the existing network-free suite from this ticket's two changes; the new script itself is out of scope for this network-free pass (per the order) since it requires live network to fully exercise.
    Additionally, ad-hoc `classify()` verification against three hand-constructed `CompletedProcess`-shaped inputs (run via a one-off `python -c` snippet, not committed anywhere):
      1. returncode=1, stdout containing a pip-audit-style "Found 1 known vulnerability in 1 package" table, no network-error text → `VULNERABILITIES_FOUND` — matched.
      2. returncode=0, stdout "No known vulnerabilities found" → `CLEAN` — matched.
      3. returncode=1, stderr containing a `ConnectionError` / "Temporary failure in name resolution" traceback (pip-audit's own connection-failure framing) → `SCAN_UNAVAILABLE` — matched.
    All three outcomes correct. This is not a substitute for the real pytest unit test (separate, out-of-scope ticket) but demonstrates the classifier logic works before handoff, per the order's verification instructions.

contract_notes:
  - none. No `app/schemas/` file touched, no TS type, no `docs/contracts/<area>-fields.md` field — this ticket is CI/tooling infrastructure only.

pack_corrections:
  - none. `capabilities/backend.md`'s four-file rhythm, dead-code-gate description, and "no test files touched" rule all held true for this ticket without contradiction (this ticket has no schema, and correctly does not touch `app/tests/**`).

handoff:
  - "`classify()` and the `Outcome` enum live in `scripts/audit_dependencies.py` at repo root (not under `services/quant-engine/`), imported via `sys.path` manipulation from a future pytest test — exactly as `05-technical-plan.md` § US-36.2 anticipated (\"first script in the repo to get pytest coverage\"). Signature: `classify(returncode: int, stdout: str, stderr: str) -> Outcome`. Exit-code constants `EXIT_CLEAN=0`, `EXIT_VULNERABILITIES_FOUND=1`, `EXIT_SCAN_UNAVAILABLE=3` are also module-level, for the T-36.2.2 workflow or the test-engineer ticket to branch on."
  - "`pip-audit` is invoked as `python -m pip_audit` (module invocation), NOT the `pip-audit` console script — confirmed on this Windows dev box that `pip install pip-audit` places the console script outside PATH by default. This matches the existing `-m ruff` / `-m vulture` convention already used in `scripts/detect_deadcode.py`. T-36.2.2's GitHub Actions workflow step should follow the same `-m pip_audit` invocation rather than assuming a bare `pip-audit` command is on PATH (GitHub-hosted runners may differ from this box, but there is no reason to risk it when the module form works everywhere pip does)."
  - "Live network was available in this session, so I ran both `pip-audit -r services/quant-engine/requirements.txt` and `npm audit --prefix apps/desktop --json` for real (not just the canned classify() cases) purely to confirm real exit-code/output shape before finalizing the marker list — this was local design verification, not part of the required network-free pass. **Both real scans currently report real findings** against this repo's actual pinned/locked dependencies: pip-audit flags advisories against `starlette==0.48.0`, `pypdf==6.9.1`, `python-multipart==0.0.20`, `pydantic-settings==2.13.1`, and `python-dotenv==1.1.1` (all PYSEC/GHSA IDs, all with fixed versions listed); `npm audit` flags at least one low-severity finding on a transitive `@babel/core` dependency. Per this ticket's non_goals I did not act on any of these — flagging so whoever reviews T-36.2.2's first real scheduled run does not mistake genuine, expected first-run findings for a tooling bug."
  - "Test file placement expected by the plan and left untouched per this order's explicit non_goals: `services/quant-engine/app/tests/test_audit_dependencies.py`, importing `scripts/audit_dependencies.py` via `sys.path` manipulation. Three canned cases are the minimum bar (found / clean / unavailable) — my ad-hoc verification above is a ready-made starting point for those fixtures, not a replacement for them."
  - "`scripts/audit_dependencies.py` is not covered by the dead-code gate's ruff/vulture pass (that pass only scans `services/quant-engine/app` + `vulture_allowlist.py`), matching the existing non-coverage of every other file under `scripts/` (`detect_deadcode.py`, `manage_cache.py`, `run_all_tests.py` itself) — no allowlist entry was needed and none was added."

risks:
  - "I installed `pip-audit==2.10.1` into this session's active Python interpreter to observe real CLI output/exit-code shape before writing `classify()`'s network-error marker list — this is a local dev-environment install (now satisfied by the `requirements-dev.txt` change for a fresh install), not an out-of-scope repo edit, but it means this box's environment is not identical to a completely fresh clone until someone runs `pip install -r requirements-dev.txt`."
  - "`classify()`'s `SCAN_UNAVAILABLE` detection is a text-marker heuristic over combined stdout+stderr, not a structural distinction — both `pip-audit` and `npm audit` share the same nonzero exit code for 'vulnerabilities found' and 'the scan itself failed', so text-matching against known connection/timeout phrasing is the only signal available short of parsing each tool's specific error taxonomy. Confirmed against pip-audit 2.10.1's actual `ConnectionError` traceback framing and standard `ENOTFOUND`/`ECONNREFUSED`-style network error text; I did not have an actual npm-network-failure example to test against live (network was in fact reachable throughout this session), so that half of the marker list is written from documented npm/Node network-error conventions rather than an observed real npm failure. Flagging as a maintenance risk for whoever writes the unit test and for future pip-audit/npm-audit version bumps that might change error wording."
  - "The work order I received explicitly excludes writing `scripts/audit_dependencies.py`'s unit test ('do not write the unit test... that is a separate test-engineer ticket, dispatched after this one'), while `05-technical-plan.md` § US-36.2 itself suggests folding that same test into this same backend order ('Fold this test into the same backend-engineer order as T-36.2.1'). I followed the work order's `non_goals` over the plan document, since the dispatched order is this lane's actual scope fence per `PROTOCOL.md` — flagging the discrepancy explicitly so the orchestrator dispatches the test-engineer ticket rather than assuming it already happened here."
  - "I did not touch `services/quant-engine/requirements.txt` even though the live pip-audit run surfaced real advisories against several of its exact-pinned versions — correctly out of scope per this ticket's non_goals ('do not act on any vulnerability finding'), but noting it here so it is not lost before T-36.2.2 or a follow-up ticket addresses it."
