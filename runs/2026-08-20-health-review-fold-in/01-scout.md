REPORT 2026-08-20-health-review-fold-in/01
status:      DONE
verdict:     NONE

changed:
  - none (read-only)

verification:
  command:   NONE
  result:    NOT_RUN
  detail:    Order was read-only recon; no verification command named.

contract_notes:
  - none

pack_corrections:
  - <agenticRoot>/projects/portfolio/project.md — "Mechanical gates — never bypass" section states: "The git-level `scripts/githooks/pre-commit` (wired via `core.hooksPath`, calling `scripts/hooks/git_pre_commit.py`) is the actual boundary — fires on every `git commit` regardless of invoking tool... a commit through another tool used to walk past it (fixed 2026-08-20)." This is FALSE as of current repo state: neither `scripts/githooks/` (any file) nor `scripts/hooks/git_pre_commit.py` exists anywhere in the repo (confirmed via Glob on `scripts/**` and repo-wide grep for `githooks`/`git_pre_commit`, both returning only the doc files that assert it). Replacement wording: remove the "actual enforcement boundary"/"fixed 2026-08-20" claim; state instead that only the Claude Code PreToolUse hook (`scripts/hooks/pre_commit_gate.py`, matched on `Bash` only) exists, and that it does not fire for `git commit` issued through a non-Bash tool (e.g. the PowerShell tool) — F-R1 is open, not fixed.

handoff:
  - "Ground truth vs the findings doc's own 'corrected' Disposition table" (the single most important thing for the producer): the findings doc's 2026-08-20 correction claims F-R1/F-R3/F-R4/F-R5/F-R6/F-R7 are "Open, logged in docs/tech-debt-register.md." A full read + repo-wide grep for "F-R" found **zero** matches in `docs/tech-debt-register.md` or anywhere under `docs/` except the findings doc itself. None of F-R1, F-R3, F-R4, F-R5, F-R6, F-R7, F-R8 are logged in the tech-debt register or referenced in `epic-roadmap.md`'s "Open items" (which lists only US-26.3, US-26.4, Epic 34's F-1a/F-10/F-12, and the Epic-35-closed cache note). So the "corrected" disposition table is ITSELF inaccurate on the "logged" claim for 6 of 8 findings — the correction pass verified the *code* facts but not that the tech-debt-register edits actually happened, repeating (on a smaller scale) the same failure mode it was created to fix.
  - F-R1 (commit gate tool-dependent) — CONFIRMED STILL TRUE AND WORSE THAN THE FINDINGS DOC CLAIMS. `.claude/settings.json:19-29` PreToolUse hook `matcher: "Bash"` only, calling `scripts/hooks/pre_commit_gate.py` (confirmed, full file read). `scripts/githooks/` directory and `scripts/hooks/git_pre_commit.py` DO NOT EXIST (Glob `scripts/**` lists only `opencode_handoff.py`, `run-all-tests.ps1`, `run_dev.py`, `detect_deadcode.py`, `run_all_tests.py`, `refresh_statement.py`, `manage_cache.py`, `hooks/schema_edit_reminder.py`, `hooks/pre_commit_gate.py` — no `githooks/`, no `git_pre_commit.py`). `.git/hooks/pre-commit` also does not exist (Glob miss). Yet **`CLAUDE.md`'s own current text** (repo root, read via the bound system reminder) now asserts a fabricated fix: "The git-level `scripts/githooks/pre-commit`... is the actual enforcement boundary... fires on every `git commit` regardless of which tool... (fixed 2026-08-20; see docs/product/review-2026-08-20-findings.md F-R1)." This is the opposite of what the findings doc's own Disposition table claims happened ("CLAUDE.md corrected to stop claiming otherwise") — CLAUDE.md was NOT corrected to remove the false claim; it currently contains an even more specific and equally false claim. The identical false claim is also in `.agentic/projects/portfolio/project.md`'s "Mechanical gates" section (see pack_corrections). Not logged in `docs/tech-debt-register.md` (grep for pre_commit_gate/commit gate/PreToolUse/tool-dependent in that file: zero matches).
  - F-R2 (currency fabrication) — CONFIRMED, matches the finding exactly. `services/quant-engine/app/services/portfolio_snapshot_builder.py:43`: `currency=item.currency or request.base_currency or 'USD'`. This is a genuine, pre-existing duplicate of tech-debt-register.md's US-26.3 entry (present verbatim, logged "2026-08-11"), which describes the identical line/behavior/rationale. No new record needed — this is the one line in the original review's disposition table that was true, and remains true.
  - F-R3 (no dependency scanning) — CONFIRMED. `.github/workflows/ci.yml` (full file read) runs only `pip install` + `npm ci` + `python scripts/run_all_tests.py` — no `pip-audit`/`npm audit` step. `.github/dependabot.yml` does not exist (Glob on `.github/**` returns only `workflows/ci.yml` and `PULL_REQUEST_TEMPLATE.md`). Not logged anywhere in `docs/tech-debt-register.md` (grep miss) or `epic-roadmap.md`.
  - F-R4 (`portfolio_return_trust` missing from dashboard-fields.md) — **FINDING IS FALSE AS OF CURRENT REPO STATE.** `docs/contracts/dashboard-fields.md:289` contains a full, detailed entry: "`range_metrics[*].portfolio_return_trust` (US-34.2 / Epic 34 F-1): trust of that range's `time_weighted_return_pct` **and** `max_drawdown_pct`..." — this is not a stub, it is a substantive paragraph matching the schema doc's own comment in `services/quant-engine/app/schemas/dashboard_history.py:37-50`. The finding's central factual claim ("has zero mentions in dashboard-fields.md") does not hold today. Whether this was already present when the review was written (and the review simply missed it) or was added since cannot be determined without git history access (no Bash/git tool available to this scout — see risks). Either way: no action is needed on this finding as stated; the producer should not fold it into the next epic without re-confirming, since the doc gap it names does not currently exist.
  - F-R5 (cache-fields.md stale) — CONFIRMED. `docs/contracts/cache-fields.md:9` header reads "Last updated: 2026-06-05". Prose at line 55-57 still says "a specific namespace removes only `<namespace>-*.json`" with no mention of US-35.2's live namespace enumeration or typo-rejection behavior. Not logged in tech-debt-register.md or epic-roadmap.md.
  - F-R6 (route inventory undercount) — CONFIRMED, exact match. `docs/product/current-product-state.md:96-108` says "12 route modules" and lists exactly the 12 named in the finding (exposure, dashboard_history, diagnostics, drift, attribution, correlation, stress, drawdown, distribution, imports, market_data, health). Actual `services/quant-engine/app/api/routes/` directory (Glob) has 15 route modules (excluding `__init__.py`): the 12 listed plus `cache.py`, `currency_risk.py`, `provenance.py` — confirmed all three exist and are wired in `app/api/main.py:4` (imported) and `main.py:18-20+` (routers included). Not logged anywhere.
  - F-R7 (stale "Active" PRD header) — CONFIRMED. `docs/product/prd/epic-24-codebase-improvement.md:3` reads "**Status:** Active (started 2026-06-19)". `docs/product/epic-roadmap.md:971` has a section header "## Completed Epic: Epic 24 — Codebase Improvement" — confirmed complete in the roadmap, confirming the PRD header is stale. Not logged anywhere.
  - F-R8 (unauthenticated local file-read) — CONFIRMED as described. `services/quant-engine/app/api/routes/imports.py:17-34`: `InteractiveBrokersImportRequest.statement_path`/`statement_paths` accepted with no path restriction; `_resolve_statement_paths` only checks existence, not location. `app/api/main.py:11-17`: CORS `allow_origins=["http://localhost:5173","http://127.0.0.1:5173"]` (not wildcard), confirming the finding's CORS claim. `docs/architecture/system-architecture.md` "API Boundary" section (lines 241-250, full section read) has no mention of file-read scope, imports.py, CORS, or "unauthenticated" — confirmed the one-line note was never added. Not logged as a tech-debt-register or roadmap item (by design, per the finding, since it's framed as an accepted tradeoff needing only documentation).
  - Overlap/dedup note for the producer: only F-R2 overlaps a tracked item (US-26.3, confirmed real and matching). F-R1, F-R3, F-R4 (now believed false), F-R5, F-R6, F-R7, F-R8 have no existing tracked record anywhere in `docs/tech-debt-register.md` or `docs/product/epic-roadmap.md`'s "Open items". None of Epic 34's deliberately-open findings (F-1a/F-10/F-12) or Epic 32's scope overlap any of F-R1/F-R3/F-R5/F-R6/F-R7/F-R8 — those are unrelated areas (investor-economics publication policy vs. commit-gate/CI/doc-drift).
  - Route file count for the producer's own math: `services/quant-engine/app/api/routes/` currently has 15 `.py` modules (excluding `__init__.py`): `health.py`, `market_data.py`, `exposure.py`, `dashboard_history.py`, `diagnostics.py`, `imports.py`, `drift.py`, `attribution.py`, `stress.py`, `drawdown.py`, `distribution.py`, `correlation.py`, `provenance.py`, `cache.py`, `currency_risk.py`.

risks:
  - This scout session had no Bash/git tool available (only Read/Write/Glob/Grep), so the work order's request to inspect `git log`/`git diff` for commits `ec4ad01`, `5a31d23`, `d34e210`, `7b0d465` (to trace when the false F-R1 "fixed" claim entered/left CLAUDE.md) could NOT be performed. All findings above are verified against current on-disk file state only (Read/Glob/Grep), which is arguably stronger ground truth for "what is true right now," but the producer should not assume I traced the historical narrative — I did not. Recommend a follow-up with git-log access if the *history* of the false claim (not just its current presence) matters for the epic write-up.
  - F-R4's status is the one genuine surprise: either the review was wrong when written, or the doc was fixed since without a corresponding tech-debt-register/roadmap entry (which would itself be a minor process gap, but not the same as an open contract-doc defect). I could not determine which without git blame/log access (same tool limitation as above). Treat F-R4 as "needs re-verification before inclusion," not as "confirmed open."
  - `docs/product/epic-roadmap.md` is 1558 lines; I read only the first 378 (top summary + Epic 35 + start of Epic 34 slice log) plus a targeted grep for "Epic 24" and "F-R". I did not read the full slice log for every epic, so I cannot rule out an F-R-adjacent mention deep in the file under different wording (e.g. spelled out prose rather than the "F-R#" token) — the grep for the literal string "F-R" across all of `docs/` is exhaustive for the exact token, but not for a paraphrase.
  - `docs/architecture/testing-architecture.md` (named in the work order's `report_to`-adjacent context via CLAUDE.md's "how does testing actually work" pointer) was not read — it is not in the work order's explicit scope list and none of the 8 findings pointed there, but the producer should know it was not checked for a stray mention of the git-level hook fiction.
