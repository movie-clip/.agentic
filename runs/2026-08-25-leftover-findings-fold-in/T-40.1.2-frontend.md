REPORT 2026-08-25-leftover-findings-fold-in/T-40.1.2
status:      DONE
verdict:     NONE

changed:
  - apps/desktop/src/features/portfolio/variantLabels.ts — added `resolveNodeImportDate`; `formatVariantNodeLabel` appends ` (YYYY-MM-DD)` when a date resolves

verification:
  command:   cd apps/desktop && npx tsc --noEmit
  result:    PASS
  detail:    clean run, zero type errors (only npm update notices in output)

contract_notes:
  - none

pack_corrections:
  - none

handoff:
  - variantLabels.test.ts (T-40.1.4) test cases are enumerated in 05-technical-plan.md § T-40.1.4 — see that section.
  - resolveNodeImportDate is exported alongside the two existing functions — same file, no new module.

risks:
  - none
