REPORT 2026-09-09-risk-annualized-volatility/08
status:      DONE
verdict:     NONE

changed:
  - services/quant-engine/app/tests/test_risk_tab_annualized_volatility.py — NEW backend module, 18 tests: the pure `_build_risk_tab_annualized_volatility` classifier (N=0, N in [1,59], N>=60, zero-variance ruling, byte-identity, withheld!=unavailable, floor constant) plus 4 integration tests via `run_diagnostics_engine` / `build_unavailable_diagnostics_result`.
  - apps/desktop/src/features/portfolio/AnnualizedVolatilityCard.test.tsx — NEW, 10 tests: synthetic figure + Synthetic badge + methodology reachability, withheld EmptyState with "N of 60" copy and no number/zero/dash/badge, unavailable EmptyState distinct from withheld, null prop -> LoadingState.
  - apps/desktop/src/features/portfolio/RiskPanel.test.tsx — added the now-required `diagnosticsAnalysis` prop to all 6 render sites; renamed the "three cards" test to four, asserting the volatility card is the 4th `.compact-chart-panel` in `.risk-shell-stack` after VarDistributionCard and that it fires no engine call.
  - apps/desktop/src/test/portfolioFixtures.ts — added `risk_tab_volatility` (trust `withheld`, value null, observations 2, minimum_observations 60) to `createImportedDiagnosticsFixture`, satisfying the now-required field for that fixture and every builder that spreads it.
  - apps/desktop/src/test/designSystem.audit.test.ts — added `AnnualizedVolatilityCard.tsx` to `ALL_CARD_FILES` and `CARDS_WITH_BADGE`.

verification:
  command:   python scripts/run_all_tests.py
  result:    PASS
  detail:    Exit 0. Backend pytest 1001 passed (was 983; +18 new), 50 warnings. Frontend vitest 369 passed / 41 files (incl. AnnualizedVolatilityCard.test.tsx 10, RiskPanel.test.tsx 6, designSystem.audit 5). tsc --noEmit clean. Dead-code gate clean (ruff / vulture / knip). Dashboard goldens regenerated by the suite with no drift (git diff on dashboardGoldens.ts empty).

contract_notes:
  - none

pack_corrections:
  - none

handoff:
  - Backend diagnostics tests have no dedicated module — they live in the 8000-line test_analytics.py. New coverage went into a focused module, services/quant-engine/app/tests/test_risk_tab_annualized_volatility.py, not appended there.
  - No new shared scaffolding: fixtures.py untouched; the new backend module reuses `imported_snapshot` / `position` and the conftest autouse `_mock_price_rows` diagnostics mock. A local `_risk_summary(...)` builder is private to the new module.
  - Cross-surface fixture is `createImportedDiagnosticsFixture` in apps/desktop/src/test/portfolioFixtures.ts; it diverges at N=2 (observations=2 -> `risk_tab_volatility` withheld/null, `volatility_summary.portfolio_volatility_pct` stays 18.2).
  - Byte-identity at N>=60 asserted two ways: `.hex()` equality vs `risk_summary.portfolio_volatility_pct` in the pure test, and `==` vs both `volatility_summary` and `risk_summary` scalars in the same `run_diagnostics_engine` result.
  - AC4/AC5 covered by `test_run_diagnostics_engine_publishes_and_equals_dashboard_at_or_above_floor` and `test_published_value_is_byte_identical_to_risk_summary_scalar` (backend) plus the divergent shared fixture.
  - AC6 covered by backend `test_at_or_above_floor_publishes_synthetic_number`, frontend synthetic block, and RiskPanel four-cards test.
  - AC7 covered by backend `test_below_floor_is_withheld_and_never_exposes_a_value` and frontend "renders no number, no zero, no dash and no trust badge".
  - AC8 covered by frontend "shows a withheld empty state that names the 60-day requirement and 'N of 60'".
  - AC9 covered by backend `test_zero_observations_is_unavailable_with_null_value` and `test_build_unavailable_diagnostics_result_marks_risk_tab_volatility_unavailable`, plus the frontend unavailable block.
  - AC10 covered by backend `test_withheld_serializes_distinct_from_unavailable` and frontend "withheld vs unavailable are visibly distinct".
  - AC11 covered by backend `test_n_equals_one_never_publishes_a_zero_on_this_field` and frontend "keeps a genuinely computed zero as 0.00%".
  - AC12 covered by frontend "makes a methodology reference reachable from the surface (AC 12)".
  - Human ruling encoded in backend `test_zero_variance_series_at_floor_publishes_0_pct_not_withheld` and frontend "keeps a genuinely computed zero as 0.00% (never withheld)"; no withheld branch for zero variance.
  - Suite regenerated apps/desktop/src/test/dashboardGoldens.ts with no net change; left as-is.

risks:
  - The two backend integration tests depend on the conftest autouse `_mock_price_rows` cadence: ~6 months of history (2025-01-02..2025-06-30) must yield >= 60 paired observations and ~3 weeks (..2025-01-24) must yield 2..59. If that synthetic generator's trading-day cadence changes, the `>= FLOOR` / `2 <= n < FLOOR` bounds could break — they assert ranges, not exact counts, to soften this.
  - `TrustBadge` was not extended to `withheld` (07 declined it). Tests assert the withheld/unavailable distinction purely via `EmptyState` title + detail copy and the absence of `.attribution-trust-badge`. If a reviewer later adds a `withheld` badge, the "no trust badge" assertions in the withheld block will need revisiting.
  - The withheld copy strings ("{N} of {min} paired portfolio and benchmark trading days available", "fewer than {min} paired trading days", "Dashboard shows an unfloored estimate") are pinned by substring match in AnnualizedVolatilityCard.test.tsx and must move in lockstep with docs/contracts/risk-fields.md (07 contract_notes) at docs close-out.
  - Capability pack gave no warning that the frontend diagnostics fixtures (`createDiagnosticsEngineFixture`, `createIb2026DiagnosticsEngineFixture`, `createFf2026DiagnosticsEngineFixture`) all derive from one object literal in `createImportedDiagnosticsFixture`, so a single field addition there satisfies tsc for all of them — only line ~994 flags under tsc.
