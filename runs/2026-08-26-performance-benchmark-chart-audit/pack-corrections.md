# Pack corrections — 2026-08-26-performance-benchmark-chart-audit

- **write-tests/SKILL.md, Recharts section** — currently documents only textual smoke checks (selector buttons, empty-state, selector-change) for chart components. Gap: no guidance for asserting exact numeric chart-line values when a chart's data-transform function is unexported and the source file is out of scope to touch (the common case for a regression-guard test written against someone else's fix). Suggested addition: document the local `vi.mock('recharts', ...)` pattern used in `apps/desktop/src/features/portfolio/PerformanceBenchmarkCard.test.tsx` (dispatch 04 of run 2026-08-26-performance-benchmark-chart-audit) — stub `LineChart` to expose its `data` prop via a `data-testid`, read via an async `getChartData()` helper — as the reference technique for exact-value chart assertions. Source: `04-test.md` § risks.
