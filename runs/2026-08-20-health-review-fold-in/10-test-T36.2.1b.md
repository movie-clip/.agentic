REPORT 2026-08-20-health-review-fold-in/10
status:      DONE
verdict:     NONE

changed:
  - services/quant-engine/app/tests/test_audit_dependencies.py — NEW. Unit-tests `scripts/audit_dependencies.py`'s `classify(returncode, stdout, stderr) -> Outcome`, imported via `sys.path` insertion of the repo-root `scripts/` dir (no `__init__.py` there, no existing precedent in this repo — first script under `scripts/` to get pytest coverage, as order 07's handoff anticipated). 10 test cases, all canned `CompletedProcess`-shaped triples, no live network: a `pytest.mark.parametrize` table covering pip-audit-found, npm-audit-found (both → `VULNERABILITIES_FOUND`), pip-audit-clean, npm-audit-clean (both → `CLEAN`), pip-audit's real `ConnectionError`/"Temporary failure in name resolution" traceback, and npm's real `ENOTFOUND`/`getaddrinfo` error framing (both → `SCAN_UNAVAILABLE` — the npm-network-failure case order 07's report flagged as untested against a real npm failure). Plus 4 dedicated boundary tests: `SCAN_UNAVAILABLE` never reads as `CLEAN`, `SCAN_UNAVAILABLE` never reads as `VULNERABILITIES_FOUND` even when stdout also carries "found" phrasing (the marker check runs before the exit-code fallback — this is AC5's core property), the marker match is case-insensitive, and an empty-output/zero-exit case falls through to `CLEAN` rather than either other state by default.

verification:
  command:   cd services/quant-engine && pytest app/tests/test_audit_dependencies.py -v
  result:    PASS
  detail:    "10 passed in 3.21s" — all parametrize ids green (pip_audit_found, npm_audit_found, pip_audit_clean, npm_audit_clean, pip_audit_connection_error, npm_audit_network_error) plus the 4 dedicated boundary tests. Also ran `python scripts/run_all_tests.py` (full gate) from repo root: backend pytest "792 passed, 45 warnings" (up from 782 in order 07's baseline — exactly the 10 new tests, confirming they ran as part of the real suite, not just in isolation), frontend vitest "37 passed (37)" / "331 passed (331)" (unchanged), dead-code gate (ruff+vulture+knip) clean, tsc clean, final line "All tests passed." `git status --porcelain` after both runs shows `apps/desktop/src/test/dashboardGoldens.ts` absent from the diff — goldens unaffected by this change, nothing to discard.

contract_notes:
  - none. No schema, route, or contract-doc field touched — this is a pure-function unit test against an existing, already-implemented classifier.

pack_corrections:
  - none. `capabilities/testing.md`'s fixture-reuse and containment-assertion guidance didn't directly apply here (this module is a self-contained pure-function suite with nothing to mock and no extensible dict to assert against), but nothing in the pack was contradicted; the REPO_ROOT-via-`parents[4]` pattern used by `test_docs_paths.py` (the nearest existing precedent for "reach outside `app/`") worked as expected and is what this file follows for `sys.path` setup.

handoff:
  - "New test file: `services/quant-engine/app/tests/test_audit_dependencies.py`. No new shared fixture was added to `app/tests/fixtures.py` — `classify()` needed none (pure function, no portfolio/market-data shape involved). If a future ticket adds more classifier scenarios, follow this file's existing `CASES` list-of-tuples + `pytest.mark.parametrize(..., ids=...)` pattern (matches `test_docs_paths.py`'s parametrize-with-ids style) rather than adding ad hoc standalone test functions, to keep failures self-naming."
  - "The npm-network-error stderr text used here (`npm error code ENOTFOUND` / `npm error syscall getaddrinfo` / `getaddrinfo ENOTFOUND registry.npmjs.org`) is written from documented npm/Node network-error conventions, same caveat order 07's `risks` section already raised: neither of us has an actual failed-npm-audit transcript from this repo's environment (network was reachable in both sessions) to confirm byte-for-byte wording. If a real scheduled run of the future `dependency-audit.yml` workflow (T-36.2.2) ever hits a genuine npm network failure, worth diffing the real stderr against `_NPM_AUDIT_NETWORK_ERROR_STDERR` in this file and against `_UNAVAILABLE_MARKERS` in `scripts/audit_dependencies.py` to confirm the marker list still matches real npm wording, not just the documented convention."
  - "`scripts/audit_dependencies.py`'s docstring (line 26) already points at this exact test file path as the intended location — this delivers that pointer; no other doc needed updating for it."

risks:
  - "This test file is NOT wired into `run_all_tests.py`'s dead-code gate scope differently from any other `app/tests/**` file — it IS scanned (ruff+vulture, since it lives under `services/quant-engine/app`), and the full-suite run above confirms it passed that gate clean. `scripts/audit_dependencies.py` itself remains outside the dead-code gate's scan root (per order 07's handoff, unchanged by this ticket) — that asymmetry (the test is scanned, the code under test is not) is pre-existing and out of this ticket's scope to fix, but flagging since it means a genuinely-dead helper added to `scripts/audit_dependencies.py` in the future would not be caught by the strict gate the way a helper under `app/` would be."
  - "I did not add a case exercising `main()`'s cross-ecosystem priority logic (VULNERABILITIES_FOUND beats SCAN_UNAVAILABLE when the two ecosystems disagree) — that logic lives in `main()`, not `classify()`, and the work order's scope/DoD names only `classify()`. Flagging in case a future ticket wants that priority rule pinned too; it would need a `main()`-level test (mocking `_run_pip_audit`/`_run_npm_audit`) rather than more `classify()` cases, since `classify()` only ever sees one ecosystem's result at a time."
