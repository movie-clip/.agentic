REPORT 2026-08-20-health-review-fold-in/02
status:      DONE
verdict:     NONE

changed:
  - none (read-only; report is this file)

verification:
  command:   NONE
  result:    NOT_RUN
  detail:    Order was read-only (producer brief); no verification command named.

contract_notes:
  - none

pack_corrections:
  - <agenticRoot>/projects/portfolio/project.md, "Mechanical gates — never bypass" section (confirmed by my own read, matches scout's 01-scout.md pack_corrections entry exactly): the text "The git-level `scripts/githooks/pre-commit` (wired via `core.hooksPath`, calling `scripts/hooks/git_pre_commit.py`) is the actual boundary — fires on every `git commit` regardless of invoking tool... a commit through another tool used to walk past it (fixed 2026-08-20)" is FALSE — `scripts/githooks/` and `scripts/hooks/git_pre_commit.py` do not exist in the repo. Replacement wording: state that only the Claude Code PreToolUse hook (`scripts/hooks/pre_commit_gate.py`, matched on `Bash` only) exists today, that it does not fire for `git commit` issued through a non-Bash tool (e.g. PowerShell), and that F-R1 is open, not fixed, pending the epic proposed below.
  - Repo root `CLAUDE.md`, "Mechanical gates" section: carries the identical false "fixed 2026-08-20" claim, with an added false citation to `docs/product/review-2026-08-20-findings.md` F-R1 (which itself, per that doc's own "Disposition" table, claims the same false fix). Same replacement as above, applied to this file. This is CLAUDE.md, not `docs/**` — flagging the ownership question explicitly in `open_decisions` below since it falls outside the docs lane's normally-scoped paths.

handoff:
  - DELIVERY BRIEF
    verdict:        PARTIAL FOLD-IN — 1 urgent doc-correction (pre-epic), 1 proposed epic (3 stories), 1 dedup, 1 drop
    epic:           PROPOSED — no existing epic covers this; roadmap's "next epic is unscoped" slot is the right home
    rationale:      Seven of the eight findings have no existing tracked record anywhere (confirmed by scout's grep of tech-debt-register.md and epic-roadmap.md, and my own grep of the story index) — this is new roadmap work, not a placement into flight. The one that does overlap (F-R2) is a confirmed exact duplicate and needs no new record. This closely matches Epic 32's own precedent (findings-first doc/gate-hygiene epic, closed 2026-08-19) rather than a normal feature epic — I am proposing a sibling to Epic 32, not a new pattern.

    stories:
      1. PROPOSED (US-<epic>.1) — "The researcher can trust that a blocked commit stays blocked, regardless of which tool issued it."
         value:      Today the test-freshness gate is enforced by a PreToolUse hook matched on `Bash` only; a `git commit` issued through the PowerShell tool (available in this same environment) bypasses it silently, with no `--no-verify`-style signal. Closing this restores the guarantee CLAUDE.md and the project pack both currently (falsely) claim already holds.
         slice:      In scope: make the freshness gate tool-independent (F-R1's two named options — mirror the PowerShell matcher, or move the check to a real git-level `pre-commit` hook via `core.hooksPath` — is a design decision for the tech lead, not decided here) plus a regression test in the spirit of Epic 32's `test_docs_paths.py` (a mechanical check, not a one-time fix, since this project's own house lesson from Epic 32 is that hand-fixed claims drift back). Out of scope: re-litigating what the gate itself checks (freshness of `.claude/.last-test-pass`) — that mechanism is not in question, only its tool-coverage.
         depends_on: none technically, but should be sequenced first (risk-first) — see sequence below and the urgent pre-epic action.
         invest:     Weak on "user-visible" in the literal sense (no researcher-facing UI changes) — justified the same way Epic 32/34/35 were: the "user" is the researcher's ability to trust the product's own integrity claims, and this project has an established precedent of epics scoped this way for exactly this reason (mechanical-gate and doc-trust stories, not feature stories).
      2. PROPOSED (US-<epic>.2) — "CI flags a newly-vulnerable pinned dependency instead of staying silent forever."
         value:      Backend deps are pinned exact, frontend deps are lockfile-pinned; nothing currently re-checks either against known vulnerabilities, and the network-free CI design means this has to be an explicit step, not something CI does implicitly.
         slice:      In scope: one dependency-vulnerability scan step (`pip-audit` / `npm audit` or equivalent), decided by tech lead re: whether it must stay network-free (a real constraint per CLAUDE.md — likely needs to run as a separate, non-blocking, possibly-scheduled check rather than inside the network-free `run_all_tests.py` gate). Out of scope: acting on any vulnerability the scan finds — that is a follow-up if and when one is found.
         depends_on: none.
         invest:     "Independent" and "small" — clean. Weak on "estimable" until the tech lead confirms whether a network-free CI can run this at all (open decision below) — that constraint might turn this into two stories (add scan infra vs. schedule it) or reshape it entirely.
      3. PROPOSED (US-<epic>.3) — "The docs an agent is told to trust for 'what's shipped' actually match the repo." (bundles F-R5, F-R6, F-R7, F-R8)
         value:      Mirrors Epic 32's US-32.1/US-32.3 pattern exactly: `cache-fields.md` still describes pre-US-35.2 CLI behaviour, `current-product-state.md` undercounts the route inventory by 3 modules (missing `cache.py`, `currency_risk.py`, `provenance.py` — all shipped, all wired), Epic 24's PRD still says "Active" though the roadmap has it Completed, and the accepted-tradeoff note about the unauthenticated local file-read (F-R8) was never added to `system-architecture.md`. Every one of these is exactly the class of doc-accuracy gap Epic 32 exists to catch, on docs Epic 32 didn't happen to touch.
         slice:      In scope: the four doc corrections themselves, plus — following Epic 32's own stated lesson ("a document that admits it cannot be trusted is a document that should be checked mechanically") — considering whether a cheap mechanical check (e.g. asserting the route module count/list in a test, the way `test_docs_paths.py` checks path claims) is worth adding for the route-inventory class specifically, since it's the kind of count that silently drifts every time a route ships. Also in scope: retiring `docs/product/review-2026-08-20-findings.md` once its findings are folded into this epic's PRD (per the product capability pack's explicit instruction not to let a standalone findings file be the record) — see open decision on what "retiring" means here given the file's own second error (below).
         depends_on: none.
         invest:     Clean vertical-ish slice for a hygiene story (precedented by Epic 32). Bundling F-R5/6/7/8 into one story rather than four is deliberate — none is independently sized enough to be its own story, and Epic 32 bundled its doc fixes the same way.

    sequence:
      0. (URGENT, PRE-EPIC, does not wait for epic approval) Correct the false "fixed 2026-08-20" claim in both `CLAUDE.md` and `<agenticRoot>/projects/portfolio/capabilities/../project.md` (see pack_corrections above). This is live misinformation feeding every agent session that binds to this project right now, including this one and the one that wrote this review's own "Correction" section — which itself reproduced a materially similar error (claiming 6 findings were "logged in docs/tech-debt-register.md" when none are). Recommend dispatching this as an express-lane doc fix immediately, in parallel with the human's epic-approval decision, not sequenced behind it.
      1. Story 1 (F-R1 real fix) — risk-first: this is the most severe finding (a security-relevant gate whose bypass is currently undetectable, not just an unfixed gap — the "fixed" claim actively hides it from anyone who reads the onboarding docs) and the class of failure ("a fix was claimed but never happened, and nothing caught it") is the same failure mode the whole epic exists to close. Doing it first also means the epic's own PRD write-up isn't itself written against a doc claiming the problem is solved.
      2. Story 3 (F-R5/6/7/8 doc sweep) — cheap, mechanical, no design questions; do it before Story 2 so the "epic PRD folds in findings" housekeeping (retiring the standalone findings file) happens close to when the findings were verified, not stale by the time Story 2's CI-design questions get resolved.
      3. Story 2 (F-R3 dependency scanning) — last, because it's the one with a real open design question (network-free CI constraint) that may need the human's or tech lead's input before scoping is final; doesn't block or get blocked by the other two.

    open_decisions:
      - Does F-R1's real fix mirror the PowerShell matcher onto the existing PreToolUse hook, or move to a real git-level `core.hooksPath` pre-commit hook (more robust, matches what the docs currently — falsely — claim already exists)? Technical trade-off for the tech lead; flagging only that both options are named in the finding and neither is decided here.
      - Can the dependency-vulnerability scan (Story 2) run inside the deliberately network-free `run_all_tests.py`/CI gate at all, or does it need to be a separate, explicitly-network-permitted, possibly-scheduled check? This changes Story 2's shape and is worth resolving before it's ticketed.
      - Who owns the CLAUDE.md correction (Step 0)? It is repo-root, not under `docs/**`, and the docs lane's scope per `project.md`'s routing table is `docs/**` plus (at close-out only) the capabilities pack. Recommend treating CLAUDE.md as in-scope for the docs lane by the same logic as the project.md pack correction (both are onboarding docs describing the same false fact), but naming this explicitly since it's not a literal scope match.
      - What happens to `docs/product/review-2026-08-20-findings.md` once its findings are folded into the new epic's PRD? It currently contains a second, self-authored error (its "Correction" section claims 6 findings are "logged in docs/tech-debt-register.md" — none are, per both the original review's own findings text and my independent grep). Recommend marking it explicitly superseded/historical (pointing at the new epic PRD as the live record) rather than deleting it, so the sequence of who-claimed-what-when stays auditable — but this is a judgment call for whoever owns doc close-out, not decided here.
      - Epic number/name: I've referred to it as `PROPOSED` throughout; the roadmap has no other epic in flight, so it can take the next number (roadmap shows Epic 35 as most recent — next available is Epic 36) at the human's discretion.
      - F-R4 (see `already_covered`) — worth a quick git-blame check to understand whether the review was simply wrong when written or the doc was fixed since without a corresponding register/roadmap entry (itself a minor process gap)? I recommend not spending effort on this — the doc is correct today, which is what matters — but naming it since scout flagged the ambiguity and couldn't resolve it without git-log access.

    already_covered:
      - F-R2 (currency-less positions fabricate a currency) is a confirmed exact duplicate of already-tracked `US-26.3` in `docs/tech-debt-register.md` (logged 2026-08-11, matches F-R2's file/line/behavior verbatim). No new record — this is the one line in the original review's own (now-void) Disposition table that was accurate, and remains accurate.
      - F-R4 (claimed: `portfolio_return_trust` missing from `dashboard-fields.md`) is **not covered because it isn't a real gap**: scout's independent verification found a full, substantive entry for that field at `dashboard-fields.md:289`, matching the schema's own docstring. The finding's central claim does not hold against current repo state. Dropping it — no epic/story/register placement. If it resurfaces (e.g. a future field genuinely goes undocumented), it should be re-verified fresh rather than assumed still open from this review.

risks:
  - I did not independently re-verify any of scout's file:line claims (dashboard-fields.md:289, cache-fields.md:9/55-57, current-product-state.md:96-108, epic-24 PRD line 3, imports.py:17-34) beyond re-reading CLAUDE.md and project.md myself for the pack-correction entries. I am treating 01-scout.md as ground truth per the work order's explicit instruction that it supersedes the findings doc's own Disposition table.
  - Like scout, I did not check git history/blame for F-R1's or F-R4's timeline (when the false claim entered CLAUDE.md, when/whether dashboard-fields.md was actually fixed). My placement recommendations do not depend on that history, but the human should know it was not traced by either of us.
  - I read only the epic-roadmap.md top summary (lines 1-180) plus the full Epic 32 and Epic 35 sections as sequencing precedent, not the full 1557-line file. I relied on scout's exhaustive grep for the literal "F-R" token across `docs/` (confirmed zero hits outside the findings doc itself) rather than re-running it myself, and on my own grep of `docs/product/stories/README.md` for dependency-scanning/route-inventory/cache-doc keywords (no hits) to corroborate the "nothing already tracks this" call.
  - The recommendation to bundle F-R5/F-R6/F-R7/F-R8 into a single story (rather than, say, four tickets under one story vs. two smaller stories) is my judgment following Epic 32's precedent shape, not a hard rule from the product pack — the human or story-author may reasonably split it differently once scoping it.
